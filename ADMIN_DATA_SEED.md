# 🧑‍💼 Admin Data Seed

هذا المستند يوضح كيفية إنشاء حساب المشرف (Admin) تلقائيًا في Firebase باستخدام سكربت seed مدمج داخل المشروع.

## ✅ ما الذي يقوم به السكربت؟

- إنشاء مستخدم في Firebase Authentication بالبريد وكلمة المرور التي تحددها.
- تعيين الاسم (displayName) لحساب المشرف.
- إنشاء/تحديث مستند المستخدم في مجموعة `users` مع الدور `admin`.
- يمكن تشغيله بأمان عدة مرات؛ لن يتم تكرار الحساب.

## 🔧 المتطلبات

1. Node.js الإصدار 18 أو أحدث (لدعم `fetch` المدمج).
2. ملف البيئة `.env` في جذر المشروع يحتوي مفاتيح Firebase التالية:

```env
VITE_FIREBASE_API_KEY=...
VITE_FIREBASE_AUTH_DOMAIN=...
VITE_FIREBASE_PROJECT_ID=...
VITE_FIREBASE_STORAGE_BUCKET=...
VITE_FIREBASE_MESSAGING_SENDER_ID=...
VITE_FIREBASE_APP_ID=...

# بيانات حساب المشرف المراد إنشاؤه
ADMIN_SEED_EMAIL=admin@school.edu
ADMIN_SEED_PASSWORD=ChangeMe123
ADMIN_SEED_NAME=System Administrator
```

> ✋ **ملاحظة مهمة:** إذا كان الحساب موجودًا مسبقًا بنفس البريد وكلمة المرور فسيتم إعادة استخدامه وتحديث ملفه الشخصي فقط.

## ▶️ طريقة التشغيل

في جذر المشروع شغّل الأمر التالي:

```bash
npm run seed:admin
```

السكربت سيقرأ ملف `.env` تلقائيًا (وكذلك `.env.local` إذا كان موجودًا) ثم ينشئ/يحدّث حساب المشرف.

## 📦 ناتج السكربت

- عند النجاح ستظهر رسالة مشابهة:

```
✅ Admin account ready:
   Email: admin@school.edu
   UID: abc123...
   Password: ChangeMe123
🎉 Admin data seed completed successfully.
```

- عند الفشل سيعرض رسالة توضح المتغيرات الناقصة أو الخطأ القادم من Firebase (مثل `EMAIL_EXISTS` أو `INVALID_PASSWORD`).

## ♻️ إعادة التشغيل بأمان

- يمكن تشغيل السكربت عدة مرات.
- إذا تغيّرت كلمة المرور في `.env` فسيحاول تسجيل الدخول بها، وفي حال كانت خاطئة سيحاول إنشاء الحساب من جديد.
- لتغيير البريد أو كلمة المرور، حدّث القيم في `.env` وأعد تشغيل السكربت.

## 🔐 نصيحة أمنية

- بعد التشغيل الأول، قم بتغيير كلمة المرور الافتراضية من خلال Firebase Console لضمان الأمان.
- تأكد من حفظ بيانات الدخول في مكان آمن وعدم رفع ملف `.env` إلى التحكم بالإصدارات.

بالتوفيق! 🚀





